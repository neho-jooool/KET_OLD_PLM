package wt.part;

import wt.part.WTPartTypeInfo;
import com.ptc.windchill.annotations.metadata.*;

/**
* This is the 'Standard Soft Attribute' cookie class interface for the corresponding TypeManaged class.<br>
* This cookie class interface enables the TypeManaged class to have additional storage columns assigned for 'Standard Soft Attributes'.<br>
* Additional storage columns can be added  by running the AddColumns tool. (See Windchill Customizer's Guide for more details.)<br>
*
*/
@GenAsUnPersistable(
   properties={
   @GeneratedProperty(name="typeInfoWTPart", type=WTPartTypeInfo.class, initialValue="new WTPartTypeInfo()",
       accessors=@PropertyAccessors(setExceptions={}),
       constraints=@PropertyConstraints(required=true))
   })

public interface WTPartTypeInterface extends _WTPartTypeInterface{
    
}
