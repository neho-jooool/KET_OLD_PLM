package ext.ket.edm.stamping.util;

public enum ErrorWhereEnum {
    QueueReceiver("When Queue recieved"), CheckExecuted("When check already execute"), CadDownloadXmlCreate("When cad download, xml request"), XmlParser("Xml parsing"), CREOTrans("CREO trans-program"), NxTrans(
	    "NX trans-program"), Stamping("Stamping-program"), SaveResult("After stamping request save file");

    private String whereValue;

    ErrorWhereEnum(String whereValue) {
	this.whereValue = whereValue;
    }

    public String getWhereValue() {
	return whereValue;
    }
}
