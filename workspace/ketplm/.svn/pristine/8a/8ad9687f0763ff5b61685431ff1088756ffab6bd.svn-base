/* bcwti
 *
 * Copyright (c) 2008 Parametric Technology Corporation (PTC). All Rights
 * Reserved.
 *
 * This software is the confidential and proprietary information of PTC
 * and is subject to the terms of a software license agreement. You shall
 * not disclose such confidential information and shall use it only in accordance
 * with the terms of the license agreement.
 *
 * ecwti
 */

package e3ps.ews.service;

import e3ps.ews.entity.KETEarlyWarning;
import e3ps.ews.entity.KETEarlyWarningEnd;
import e3ps.ews.entity.KETEarlyWarningEndReq;
import e3ps.ews.entity.KETEarlyWarningPlan;
import e3ps.ews.entity.KETEarlyWarningResult;
import e3ps.ews.entity.KETEarlyWarningStep;
import e3ps.ews.service.KETEwsService;
import java.lang.Boolean;
import java.util.Hashtable;
import java.util.Vector;
import wt.method.RemoteAccess;
import wt.services.ServiceFactory;
import wt.util.WTException;

@Deprecated
public class KETEwsServiceFwd implements KETEwsService, RemoteAccess {


   public KETEarlyWarning createEarlyWarning( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).createEarlyWarning( hash, files );
   }

   public void createEarlyWarningTarget( Hashtable hash, KETEarlyWarning ketEarlyWarning )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      ServiceFactory.getService(KETEwsService.class).createEarlyWarningTarget( hash, ketEarlyWarning );
   }

   public KETEarlyWarning updateEarlyWarning( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).updateEarlyWarning( hash, files );
   }

   public void deleteEarlyWarningTarget( Hashtable hash, KETEarlyWarning ketEarlyWarning )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      ServiceFactory.getService(KETEwsService.class).deleteEarlyWarningTarget( hash, ketEarlyWarning );
   }

   public KETEarlyWarning reviseEarlyWarning( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).reviseEarlyWarning( hash, files );
   }

   public Boolean deleteEarlyWarning( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).deleteEarlyWarning( hash, files );
   }

   public KETEarlyWarningPlan createEarlyWarningPlan( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).createEarlyWarningPlan( hash, files );
   }

   public KETEarlyWarningPlan updateEarlyWarningPlan( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).updateEarlyWarningPlan( hash, files );
   }

   public KETEarlyWarningPlan reviseEarlyWarningPlan( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).reviseEarlyWarningPlan( hash, files );
   }

   public Boolean deleteEarlyWarningPlan( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).deleteEarlyWarningPlan( hash, files );
   }

   public KETEarlyWarningResult createEarlyWarningResult( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).createEarlyWarningResult( hash, files );
   }

   public KETEarlyWarningResult updateEarlyWarningResult( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).updateEarlyWarningResult( hash, files );
   }

   public Boolean deleteEarlyWarningResult( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).deleteEarlyWarningResult( hash, files );
   }

   public void createEarlyWarningProblem( Hashtable hash, KETEarlyWarningResult ketEarlyWarningResult )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      ServiceFactory.getService(KETEwsService.class).createEarlyWarningProblem( hash, ketEarlyWarningResult );
   }

   public void deleteEarlyWarningProblem( Hashtable hash, KETEarlyWarningResult ketEarlyWarningResult )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      ServiceFactory.getService(KETEwsService.class).deleteEarlyWarningProblem( hash, ketEarlyWarningResult );
   }

   public KETEarlyWarningEndReq createEarlyWarningEndReq( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).createEarlyWarningEndReq( hash, files );
   }

   public KETEarlyWarningEndReq updateEarlyWarningEndReq( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).updateEarlyWarningEndReq( hash, files );
   }

   public Boolean deleteEarlyWarningEndReq( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).deleteEarlyWarningEndReq( hash, files );
   }

   public KETEarlyWarningEnd createEarlyWarningEnd( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).createEarlyWarningEnd( hash, files );
   }

   public KETEarlyWarningEnd updateEarlyWarningEnd( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).updateEarlyWarningEnd( hash, files );
   }

   public Boolean deleteEarlyWarningEnd( Hashtable hash, Vector files )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).deleteEarlyWarningEnd( hash, files );
   }

   public KETEarlyWarningStep stopEarlyWarning( Hashtable hash )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).stopEarlyWarning( hash );
   }

   public KETEarlyWarningStep reopenEarlyWarning( Hashtable hash )
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).reopenEarlyWarning( hash );
   }

   public Boolean updatePartner()
            throws WTException {

      // TODO: remove all usages of this Fwd class, then delete it (see ServiceFactory info)
      // forward to remote service implementation
      return ServiceFactory.getService(KETEwsService.class).updatePartner();
   }
}
